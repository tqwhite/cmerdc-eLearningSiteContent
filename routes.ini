routes._default.route = ''
routes._default.defaults.title='E-Learning Solutions'
routes._default.defaults.defaultAnchor='Home'
routes._default.defaults.module = multiPanel			;page type
routes._default.defaults.controller = generate		;always the same
routes._default.defaults.action =container			;always the same

routes.sitemap.route = '/sitemap/'
routes.sitemap.noList = true						;noList==true prevents route from appearing on sitemap
routes.sitemap.defaults.module = siteMap		;page type
routes.sitemap.defaults.controller = generate		;always the same
routes.sitemap.defaults.action =container			;always the same

routes.contact.route = "/contact/"
routes.contact.defaults.title='Contact'
routes.contact.defaults.module = pureHtmlIntoLayout		;page type
routes.contact.defaults.controller = generate		;always the same
routes.contact.defaults.action =container			;always the same

routes.solutions.route = "/solutions/"
routes.solutions.defaults.title='Our eLearning Solution'
routes.solutions.defaults.module = pureHtmlIntoLayout		;page type
routes.solutions.defaults.controller = generate		;always the same
routes.solutions.defaults.action =container			;always the same

routes.learning.route = "/learning/"
routes.learning.defaults.title='Learning Platform'
routes.learning.defaults.module = pureHtmlIntoLayout		;page type
routes.learning.defaults.controller = generate		;always the same
routes.learning.defaults.action =container			;always the same

routes.curriculum.route = "/curriculum/"
routes.curriculum.defaults.title='Curriculum Management'
routes.curriculum.defaults.module = pureHtmlIntoLayout		;page type
routes.curriculum.defaults.controller = generate		;always the same
routes.curriculum.defaults.action =container			;always the same

routes.performance.route = "/performance/"
routes.performance.defaults.title='Performance Management'
routes.performance.defaults.module = pureHtmlIntoLayout		;page type
routes.performance.defaults.controller = generate		;always the same
routes.performance.defaults.action =container			;always the same

routes.integration.route = "/integration/"
routes.integration.defaults.title='Integration Services'
routes.integration.defaults.module = pureHtmlIntoLayout		;page type
routes.integration.defaults.controller = generate		;always the same
routes.integration.defaults.action =container			;always the same

routes.community.route = "/community/"
routes.community.defaults.title='Learning Community'
routes.community.defaults.module = pureHtmlIntoLayout		;page type
routes.community.defaults.controller = generate		;always the same
routes.community.defaults.action =container			;always the same

routes.mobile.route = "/mobile/"
routes.mobile.defaults.title='Mobile Learning'
routes.mobile.defaults.module = pureHtmlIntoLayout		;page type
routes.mobile.defaults.controller = generate		;always the same
routes.mobile.defaults.action =container			;always the same



;routes.multiPanel.route = "/multiPanel/"
;routes.multiPanel.defaults.title='Multi-Panel'
;routes.multiPanel.defaults.module = multiPanel		;page type
;routes.multiPanel.defaults.controller = generate		;always the same
;routes.multiPanel.defaults.action =container			;always the same

;routes.tmultiPanel.route = "/tmultiPanel/"
;routes.tmultiPanel.defaults.title='Multi-Panel2'
;routes.tmultiPanel.defaults.module = tabbedMulti2		;page type
;routes.tmultiPanel.defaults.controller = generate		;always the same
;routes.tmultiPanel.defaults.action =container			;always the same


;SAMPLE ROUTES =================================

;routes._default.route = ""
;routes._default.defaults.title='Home'
;routes._default.defaults.module = pureHtml		;page type
;routes._default.defaults.controller = generate		;always the same
;routes._default.defaults.action =container			;always the same
;
;routes.sitemap.route = "/sitemap/"
;routes.sitemap.defaults.title='Site Map'
;routes.sitemap.noList = true						;noList==true prevents route from appearing on sitemap
;routes.sitemap.defaults.module = siteMap		;page type
;routes.sitemap.defaults.controller = generate		;always the same
;routes.sitemap.defaults.action =container			;always the same
;
;routes.siteDirectory.route = "/siteDirectory/"
;routes.siteDirectory.defaults.title='Site Directory'
;routes.siteDirectory.noList = true						;noList==true prevents route from appearing on sitemap
;routes.siteDirectory.defaults.module = siteDirectory		;page type
;routes.siteDirectory.defaults.controller = generate		;always the same
;routes.siteDirectory.defaults.action =container			;always the same
;
;routes.iframe.route = "/iframe/"
;routes.iframe.defaults.title='iFrame'
;routes.iframe.defaults.module = iframe		;page type
;routes.iframe.defaults.controller = generate		;always the same
;routes.iframe.defaults.action =container			;always the same
;
;routes.multiPanel.route = "/multiPanel/"
;routes.multiPanel.defaults.title='Multi-Panel'
;routes.multiPanel.defaults.module = multiPanel		;page type
;routes.multiPanel.defaults.controller = generate		;always the same
;routes.multiPanel.defaults.action =container			;always the same
;
;routes.pureHtml.route = "/pureHtml/"
;routes.pureHtml.defaults.title='Pure Html'
;routes.pureHtml.defaults.module = pureHtml		;page type
;routes.pureHtml.defaults.controller = generate		;always the same
;routes.pureHtml.defaults.action =container			;always the same
;
;routes.externalUrlList.route = "/externalUrlList/"
;routes.externalUrlList.defaults.title='External URL List'
;routes.externalUrlList.defaults.module = externalUrlList		;page type
;routes.externalUrlList.defaults.controller = generate		;always the same
;routes.externalUrlList.defaults.action =container			;always the same
;
;routes.categoryFront1.route = "/categoryFront1/"
;routes.categoryFront1.defaults.title='Category Front Page #1'
;routes.categoryFront1.defaults.module = categoryFront1		;page type
;routes.categoryFront1.defaults.controller = generate		;always the same
;routes.categoryFront1.defaults.action =container			;always the same
